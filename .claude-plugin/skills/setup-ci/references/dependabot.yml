# Generated by /setup-ci skill — Dependabot 依存関係自動更新設定
# プロジェクトタイプに応じて該当セクションのコメントを解除すること

version: 2
updates:
  # --- npm (Node.js プロジェクト) ---
  # - package-ecosystem: "npm"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #   open-pull-requests-limit: 10
  #   labels:
  #     - "dependencies"
  #   groups:
  #     minor-and-patch:
  #       update-types: ["minor", "patch"]

  # --- cargo (Rust プロジェクト) ---
  # - package-ecosystem: "cargo"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #   open-pull-requests-limit: 10
  #   labels:
  #     - "dependencies"
  #   groups:
  #     minor-and-patch:
  #       update-types: ["minor", "patch"]

  # --- GitHub Actions (全プロジェクト共通) ---
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "ci"
