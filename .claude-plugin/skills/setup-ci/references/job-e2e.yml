# E2E / Integration test job snippet — appended by /setup-ci --with-e2e
# Customize services and commands to match your project

  e2e:
    name: E2E / Integration Tests
    needs: check
    runs-on: ubuntu-latest
    # --- Rust API project ---
    # services:
    #   postgres:
    #     image: postgres:16
    #     env:
    #       POSTGRES_USER: test
    #       POSTGRES_PASSWORD: test
    #       POSTGRES_DB: test
    #     ports:
    #       - 5432:5432
    #     options: >-
    #       --health-cmd pg_isready
    #       --health-interval 10s
    #       --health-timeout 5s
    #       --health-retries 5
    steps:
      - uses: actions/checkout@v4

      # --- Rust API project ---
      # - uses: dtolnay/rust-toolchain@{{TOOLCHAIN}}
      # - uses: Swatinem/rust-cache@v2
      # - name: Run integration tests
      #   id: e2e-tests
      #   run: |
      #     set -o pipefail
      #     cargo test --test '*' --quiet 2>&1 | tee /tmp/e2e-output.txt
      #   env:
      #     DATABASE_URL: postgres://test:test@localhost:5432/test

      # --- Node.js project ---
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '{{NODE_VERSION}}'
      #     cache: '{{PACKAGE_MANAGER}}'
      # - run: {{INSTALL_COMMAND}}
      # - name: Install Playwright browsers
      #   run: npx playwright install --with-deps chromium
      # - name: Run E2E tests
      #   id: e2e-tests
      #   run: |
      #     set -o pipefail
      #     npx playwright test 2>&1 | tee /tmp/e2e-output.txt

      # --- PR comment: E2E results (uncomment when configuring) ---
      # - name: Post E2E results to PR
      #   if: always() && github.event_name == 'pull_request'
      #   uses: actions/github-script@v7
      #   env:
      #     E2E_RESULT: ${{ steps.e2e-tests.outcome }}
      #   with:
      #     script: |
      #       const fs = require('fs');
      #       const e2eOutput = fs.existsSync('/tmp/e2e-output.txt')
      #         ? fs.readFileSync('/tmp/e2e-output.txt', 'utf8').trim().split('\n').slice(-30).join('\n')
      #         : 'No E2E output';
      #       const icon = (r) => r === 'success' ? ':white_check_mark:' : ':x:';
      #       const tag = '<!-- ci-e2e-result -->';
      #       const status = process.env.E2E_RESULT === 'success';
      #       const body = [
      #         tag,
      #         `## ${status ? ':white_check_mark:' : ':x:'} E2E / Integration Tests`,
      #         '',
      #         `Result: ${icon(process.env.E2E_RESULT)}`,
      #         '',
      #         '<details><summary>E2E output (last 30 lines)</summary>',
      #         '', '```', e2eOutput, '```', '',
      #         '</details>',
      #       ].join('\n');
      #       const { data: comments } = await github.rest.issues.listComments({
      #         owner: context.repo.owner, repo: context.repo.repo,
      #         issue_number: context.issue.number,
      #         per_page: 100,
      #       });
      #       const existing = comments.find(c => c.body.includes(tag));
      #       if (existing) {
      #         await github.rest.issues.updateComment({
      #           owner: context.repo.owner, repo: context.repo.repo,
      #           comment_id: existing.id, body,
      #         });
      #       } else {
      #         await github.rest.issues.createComment({
      #           owner: context.repo.owner, repo: context.repo.repo,
      #           issue_number: context.issue.number, body,
      #         });
      #       }
